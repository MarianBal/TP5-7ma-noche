import { makeStyles } from '@material-ui/styles';

export default makeStyles({
  headerContainer: {
    width: '100%',
    maxWidth: '100%',
    height: '75px',
    backgroundColor: 'red',
    borderRadius: 0,
  },
});
